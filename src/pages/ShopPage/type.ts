export type Product = {
  id: string;
  title: string;
  name: string;
  image: string;
  brand: string;
  price: number;
  description: string;
};

export type RequestStatus = {
  description: string;
  message: string;
};
